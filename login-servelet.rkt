#lang web-server/insta

;; demo servelet
;; if user is not logged in show a username/password form
;; if user is logged in show username and logout link
;; when servelet runs check for 'logged in' cookie
;; Other  